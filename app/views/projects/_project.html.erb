<div class="container">
	<td><%= link_to project.name, team_project_path(current_team,project)%> </td>
	<td style="float:right; margin-right:20px;"><%= link_to 'Edit', edit_project_path(project),remote: true, class: 'btn btn-secondary' %><%= link_to 'Delete', destroy_project_path(project), method: :delete, class: 'btn btn-danger'%></td>
</div>
